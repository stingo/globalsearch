<%= simple_form_for(@motor) do |f| %>
  <% if motor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(motor.errors.count, "error") %> prohibited this motor from being saved:</h2>

      <ul>
      <% motor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :town %>
    <%= f.input :local_area %>

  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
